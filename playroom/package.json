{
  "name": "the-pipeline-operator",
  "version": "1.0.0",
  "private": true,
  "description": "The Pipeline Operator — visual surface for a sales-pipeline operator. Reads ../CLAUDE.md, ../identity.md, ../rules.md and ../reference/ at request time to drive a real disposition via Claude. The folder is the agent; this app is just a surface.",
  "scripts": {
    "sync": "node scripts/sync-operator.mjs",
    "predev": "node scripts/sync-operator.mjs",
    "dev": "next dev",
    "prebuild": "node scripts/sync-operator.mjs",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.40.1",
    "next": "^14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "tailwindcss": "^3.4.14",
    "typescript": "^5.6.0"
  }
}
